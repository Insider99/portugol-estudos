algoritmo "repitaeenquanto"
var
contador,tabnumero:inteiro
inicio
escreval("Insira um número:")
leia(tabnumero)

//repita
//escreval(tabnumero," x",contador," =",tabnumero*contador)
//contador <- contador+1
//ate contador > 10

enquanto contador < 10 faca
contador <- contador+1
escreval(tabnumero," x",contador," =",tabnumero*contador)
fimenquanto

fimalgoritmo